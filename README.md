<div dir="rtl" align="right">

# تشخیص هویت چهره با Eigenfaces و پیاده‌سازی PCA از صفر

پروژه ریاضی دوره مهندسی یادگیری ماشین رهنماکالج - تیم ۴

## معرفی پروژه

در این پروژه می‌خواهیم بررسی کنیم آیا می‌توان یک تصویر چهره `64×64` را به‌جای ۴۰۹۶ مقدار پیکسل، با تعداد بسیار کمتری عدد نمایش داد و همچنان اطلاعات لازم برای بازسازی تصویر و تشخیص هویت را حفظ کرد.

برای این کار PCA را مرحله‌به‌مرحله با NumPy می‌نویسیم و از پیاده‌سازی آماده PCA استفاده نمی‌کنیم.

## سؤال اصلی

چند مؤلفه اصلی برای ساختن یک نمایش فشرده و قابل‌استفاده از چهره‌ها کافی است؟

## سؤال اختصاصی تیم ۴: تعریف شباهت

آیا انتخاب معیار شباهت در فضای مؤلفه‌های اصلی باعث تغییر نزدیک‌ترین همسایه و دقت تشخیص هویت می‌شود؟

در آزمایش اختصاصی تیم، دو معیار زیر را مقایسه می‌کنیم:

- فاصله اقلیدسی: `Euclidean distance`
- شباهت کسینوسی: `Cosine similarity`

### فرضیه اولیه

حدس اولیه ما این بود که Cosine به اندازه بردار حساسیت کمتری دارد، ولی از قبل نمی‌دانستیم کدام معیار بهتر است. برای پاسخ، دقت و همسایه‌های انتخاب‌شده هر دو روش را مقایسه کردیم.

## دیتاست

از دیتاست Olivetti Faces استفاده می‌کنیم:

- ۴۰۰ تصویر خاکستری
- ۴۰ هویت متفاوت
- ۱۰ تصویر برای هر هویت
- ابعاد هر تصویر: `64×64`
- تعداد ویژگی‌های هر تصویر پس از flatten: `4096`

دیتاست فقط با `sklearn.datasets.fetch_olivetti_faces` دریافت می‌شود. برای PCA یا nearest neighbor از پیاده‌سازی آماده scikit-learn استفاده نمی‌کنیم.

## روش جداسازی داده

برای هر هویت:

- ۸ تصویر برای train
- ۲ تصویر برای test

انتخاب تصاویر به‌صورت تصادفی و stratified با `random seed` ثابت انجام می‌شود تا اجرای پروژه تکرارپذیر باشد. تمام پارامترهای یادگرفتنی، از جمله mean face و eigenfaces، فقط از داده train محاسبه می‌شوند.

## مسیر اجرای پروژه

1. دریافت داده و جداسازی ۸ تصویر train و ۲ تصویر test از هر شخص
2. تبدیل هر تصویر به یک بردار ۴۰۹۶بعدی
3. محاسبه mean face از train و مرکزسازی داده‌ها
4. ساخت Gram matrix
5. محاسبه eigenvalueها، eigenvectorها و eigenfaceها
6. projection به فضای `k`بعدی
7. reconstruction برای `k = 10, 50, 150`
8. محاسبه خطای بازسازی
9. نوشتن 1-NN با Euclidean و Cosine
10. محاسبه accuracy روی test
11. مقایسه کوتاه با SVD
12. دیدن نمونه موفق، شکست و همسایه‌های متفاوت

## محدودیت‌های پیاده‌سازی

### مجاز

- NumPy برای محاسبات عددی
- Matplotlib برای نمودار و نمایش تصویر
- scikit-learn فقط برای دریافت دیتاست
- `numpy.linalg.eigh`
- `numpy.linalg.svd`

### غیرمجاز

- `sklearn.decomposition.PCA`
- پیاده‌سازی آماده nearest neighbor
- استفاده از داده test برای محاسبه mean، مؤلفه‌ها یا انتخاب پارامترهای مدل

## خروجی‌های مورد انتظار

- mean face و چند eigenface
- بازسازی برای `k = 10, 50, 150`
- نمودار cumulative variance و خطای بازسازی
- دقت Euclidean و Cosine روی test
- نمونه موفق، شکست و همسایه‌های متفاوت
- مقایسه کوتاه با SVD
- نتیجه و محدودیت پروژه

## ساختار پروژه

```text
eigenfaces-from-scratch/
├── README.md
├── requirements.txt
├── .gitignore
├── eigenfaces_from_scratch.ipynb
├── docs/
│   ├── PROJECT_PLAN.md
│   ├── LEARNING_GUIDE.md
│   ├── TEAM_GUIDE.md
│   └── PRESENTATION_PLAN.md
└── outputs/
    └── figures/
```

نوت‌بوک اصلی باید از ابتدا تا انتها با Run All و بدون مرحله دستی پنهان اجرا شود. تصاویر نهایی مورد استفاده در گزارش یا ارائه در `outputs/figures/` ذخیره می‌شوند. برای خواناماندن Pull Requestها، خروجی سلول‌ها در نسخه Git پاک نگه داشته می‌شود و صحت اجرا با Run All کنترل می‌شود.

## نصب و اجرا

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
jupyter lab
```

سپس فایل `eigenfaces_from_scratch.ipynb` را باز و Run All کنید.

اجرای نهایی فعلی با Python 3.13.1، NumPy 2.5.2، Matplotlib 3.11.1 و scikit-learn 1.9.0 کنترل شده است.

## اعضای تیم

| عضو | GitHub username | مسئولیت پیشنهادی |
|---|---|---|
| یاسمن | [@yasaman1994](https://github.com/yasaman1994) | یکپارچه‌سازی، مستندسازی، کنترل data leakage و اجرای نهایی |
| شکیب | [@Shakib1380](https://github.com/Shakib1380) | بازبینی ریاضی PCA، covariance، eigendecomposition و مقایسه با SVD |
| امین | [@Aminbyt](https://github.com/Aminbyt) | تسلط و ارائه projection، reconstruction، خطای بازسازی و نمودارها |
| صبا | [@SabaHesaraki](https://github.com/SabaHesaraki) | تسلط و ارائه nearest neighbor و مقایسه Euclidean/Cosine |

هر عضو روی بخش مشخص‌شده بیشتر تمرکز می‌کند، ولی همه باید مسیر کلی پروژه را بدانند.

## روش همکاری تیمی

- هر کار در یک branch جدا انجام می‌شود.
- تغییرها بعد از بررسی با Pull Request وارد `main` می‌شوند.
- قبل از شروع کار، آخرین نسخه `main` دریافت می‌شود.
- دو نفر هم‌زمان یک بخش از نوت‌بوک را ویرایش نمی‌کنند.

دستورهای Git در [راهنمای همکاری تیم](docs/TEAM_GUIDE.md)، تقسیم کار در [برنامه پروژه](docs/PROJECT_PLAN.md)، توضیح فرمول‌ها در [راهنمای آموزشی](docs/LEARNING_GUIDE.md) و سناریوی ارائه در [برنامه ارائه](docs/PRESENTATION_PLAN.md) آمده‌اند.

## وضعیت فعلی

- [x] دریافت و بررسی دیتاست
- [x] جداسازی تصادفی و متوازن train و test با seed ثابت
- [x] flatten تصاویر
- [x] mean face و مرکزسازی
- [x] محاسبه واریانس کل داده واقعی و کنترل حفظ آن
- [x] Gram matrix و eigendecomposition
- [x] نمایش اولیه eigenfaces
- [x] projection همه تصاویر train و test و reconstruction
- [x] reconstruction error و نمودار آن
- [x] nearest neighbor و accuracy روی test
- [x] مقایسه حداقلی با SVD
- [x] آزمایش اختصاصی Euclidean/Cosine
- [x] تحلیل نمونه موفق، شکست و همسایه‌های متفاوت
- [x] اجرای نهایی نوت‌بوک و ذخیره تصاویر ارائه
- [ ] مرور نهایی اعضا و تمرین ارائه

## نتایج اجرای فعلی

تمام نتایج زیر مربوط به split ثابت `seed=42` هستند. Cumulative variance فقط از train محاسبه شده و MSE و accuracy فقط روی test گزارش شده‌اند:

| k | Cumulative variance | Reconstruction MSE | Euclidean accuracy | Cosine accuracy |
|---:|---:|---:|---:|---:|
| 10 | 65.96٪ | 0.00706589 | 92.50٪ | 87.50٪ |
| 50 | 87.85٪ | 0.00326911 | 93.75٪ | 93.75٪ |
| 150 | 97.06٪ | 0.00205541 | 93.75٪ | 93.75٪ |

در `k=50` دو معیار برای ۵ نمونه تصویر train متفاوتی را به‌عنوان همسایه انتخاب کردند و در یک نمونه حتی برچسب پیش‌بینی آن‌ها متفاوت بود. این نتیجه نشان می‌دهد تعریف شباهت روی تصمیم اثر دارد، اما بر اساس یک دیتاست و یک split نمی‌توان گفت یک معیار همیشه بهتر است.

## وضعیت پروژه

پیاده‌سازی الزامی پروژه کامل و اجرا شده است. مرحله باقی‌مانده، مرور اعضا، انتخاب خروجی‌های ارائه و تمرین دفاع از نتیجه است.

</div>
