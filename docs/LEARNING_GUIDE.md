<div dir="rtl" align="right">

# راهنمای آموزشی و دفتر تصمیم‌های پروژه Eigenfaces

## هدف این فایل

این فایل فقط دستور انجام کار نیست. در هر مرحله توضیح می‌دهد:

1. چه مسئله‌ای داریم؛
2. چه تصمیمی گرفته‌ایم؛
3. کدام بخش الزام استاد و کدام بخش انتخاب تیم است؛
4. چرا تصمیم از نظر ریاضی یا مهندسی قابل‌دفاع است؛
5. چه جایگزین‌هایی وجود دارد؛
6. با چه آزمونی از درست‌بودن نتیجه مطمئن می‌شویم.

این سند باید هم‌زمان با نوت‌بوک به‌روزرسانی شود. اگر کد تغییر کند ولی دلیل تصمیم در این فایل تغییر نکند، مستندات قدیمی شده‌اند.

## ۱. از کجا بدانیم چیزی درست است؟

برای این پروژه سه نوع «درستی» داریم:

### ۱.۱. انطباق با صورت پروژه

ممکن است یک روش از نظر علمی درست باشد، اما با محدودیت تکلیف سازگار نباشد. برای مثال `sklearn.decomposition.PCA` معتبر است، ولی در این پروژه ممنوع است.

منبع اصلی الزامات، به‌ترتیب:

1. صورت رسمی پروژه: `Math-Project-Kickoff.pdf`
2. اسلاید جلسه: `Math-Project-Kickoff-Eigenfaces-Rahnema-College.pdf`
3. توضیح شفاهی استاد و تصمیم‌های ثبت‌شده تیم

### ۱.۲. درستی ریاضی

هر فرمول باید با ابعاد ماتریس‌ها سازگار باشد و ویژگی مورد انتظار را داشته باشد. برای مثال:

- covariance matrix باید متقارن باشد؛
- eigenfaceها باید تقریباً متعامد و با طول واحد باشند؛
- بازسازی با مؤلفه‌های بیشتر نباید خطای بیشتری ایجاد کند؛
- مسیر Gram و SVD باید تقریباً یک زیرفضا تولید کنند.

### ۱.۳. درستی آزمایش

حتی کد و فرمول درست می‌تواند نتیجه نامعتبر بسازد. مهم‌ترین خطر این پروژه data leakage است. داده test نباید در ساخت mean face، eigenfaces یا انتخاب پارامترهای مدل دخالت کند.

## ۲. جدول ردیابی الزامات

| الزام | منبع | نوع | وضعیت فعلی |
|---|---|---|---|
| ۸ تصویر train و ۲ تصویر test برای هر هویت | صورت پروژه، صفحه ۳ | اجباری | انجام و با seed ثابت تکرارپذیر شده است |
| mean فقط از train | صورت پروژه، صفحات ۳ و ۴ | اجباری | انجام شده |
| PCA از صفر با NumPy | صورت پروژه، صفحه ۴ | اجباری | هسته اولیه انجام شده |
| ممنوع‌بودن PCA و nearest neighbor آماده | صورت پروژه، صفحه ۴ | اجباری | رعایت شده |
| reconstruction برای `k=10,50,150` | صورت پروژه، صفحات ۳، ۴ و ۶ | اجباری | انجام نشده |
| accuracy و failure analysis روی test | صورت پروژه، صفحات ۳ تا ۵ | اجباری | انجام نشده |
| مقایسه حداقلی با SVD | صورت پروژه، صفحات ۳ و ۶ | اجباری | انجام نشده |
| Euclidean در برابر Cosine | صورت پروژه، صفحات ۶ و ۸ | مأموریت تیم ۴ | انجام نشده |
| نمایش همسایه‌های متفاوت دو معیار | صورت پروژه، صفحه ۸ | مأموریت تیم ۴ | انجام نشده |
| نتیجه و محدودیت | صورت پروژه، صفحات ۴ و ۱۰ | اجباری | انجام نشده |
| اجرای کامل با Run All | صورت پروژه، صفحه ۴ | اجباری | پس از تکمیل باید آزمون شود |

## ۳. تصمیم‌های ثبت‌شده تیم

### تصمیم ۱: یک نوت‌بوک نهایی یکپارچه

**نوع:** الزام استاد و تصمیم مهندسی

نسخه نهایی در `eigenfaces_from_scratch.ipynb` قرار می‌گیرد و باید با Run All اجرا شود.

**چرا؟**

صورت پروژه یک مسیر کامل از دریافت داده تا ارزیابی می‌خواهد. چند نوت‌بوک جدا ممکن است ترتیب اجرا یا وابستگی‌های پنهان ایجاد کند.

**نکته تیمی:**

اعضا می‌توانند روی branchهای جدا کار کنند، اما نباید هم‌زمان قسمت یکسانی از فایل `.ipynb` را تغییر دهند؛ merge conflict نوت‌بوک‌ها سخت خوانده می‌شود.

### تصمیم ۲: split برابر ۸/۲ درون هر هویت

**نوع:** نسبت ۸/۲ الزام استاد؛ تصادفی‌کردن و seed انتخاب تیم

برای هر شخص، اندیس‌های همان شخص را با مولد تصادفی NumPy و seed ثابت `42` جابه‌جا می‌کنیم؛ ۸ مورد train و ۲ مورد test می‌شوند.

```python
rng = np.random.default_rng(42)

for person in np.unique(labels):
    person_indices = np.where(labels == person)[0]
    shuffled_indices = rng.permutation(person_indices)
    train_indices.extend(shuffled_indices[:8])
    test_indices.extend(shuffled_indices[8:])
```

**چرا؟**

- تعداد نمونه هر هویت دقیقاً حفظ می‌شود؛
- انتخاب فقط بر اساس ترتیب اولیه فایل نیست؛
- seed ثابت باعث می‌شود تمام اعضا دقیقاً یک نتیجه بگیرند.

**جایگزین:** `train_test_split(..., stratify=labels)` نیز از نظر فنی مناسب است، ولی برای هدف آموزشی ترجیح می‌دهیم خود منطق جداسازی را با NumPy ببینیم.

**محدودیت:** نتیجه فقط مربوط به یک split است. اگر زمان داشتیم، پایداری نتیجه را روی چند seed بررسی می‌کردیم؛ اما نتیجه نهایی هر seed نباید برای دست‌کاری و بهترکردن دلخواه گزارش استفاده شود.

**آزمون صحت:**

```python
assert X_train.shape[0] == 320
assert X_test.shape[0] == 80
assert np.all(np.bincount(y_train) == 8)
assert np.all(np.bincount(y_test) == 2)
assert len(np.intersect1d(train_indices, test_indices)) == 0
```

### تصمیم ۳: نمایش هر تصویر به‌صورت یک ردیف

**نوع:** الزام مسیر پروژه

اگر `n` تصویر داشته باشیم:

```text
images: (n, 64, 64)
X:      (n, 4096)
```

**چرا؟**

در قرارداد این پروژه، هر ردیف یک sample و هر ستون یک feature یا pixel است. تمام فرمول‌های بعدی بر همین قرارداد نوشته می‌شوند.

**نکته:** flatten یک تبدیل بدون پارامتر است؛ بنابراین انجام آن قبل از split از نظر leakage اشتباه نیست. بااین‌حال، در نسخه نهایی ترتیب ظاهری نوت‌بوک را مطابق صورت پروژه، یعنی split و سپس ساخت ماتریس‌های تخت train/test، می‌چینیم.

### تصمیم ۴: استفاده از `float64`

**نوع:** تصمیم فنی تیم

تصاویر را پیش از جبر خطی به `np.float64` تبدیل می‌کنیم.

**چرا؟**

مقادیر ویژه بسیار کوچک و کنترل‌های تعامد به خطای اعشاری حساس‌اند. `float64` برای این دیتاست کوچک حافظه قابل‌قبولی دارد و خطای عددی را نسبت به `float32` کاهش می‌دهد.

**آزمون صحت:**

```python
assert X_train.dtype == np.float64
```

### تصمیم ۵: mean فقط از train

**نوع:** الزام صریح استاد و اصل جلوگیری از leakage

برای ماتریس `X_train` با شکل `(n, d)`:

```text
mu[j] = sum(X[i, j] for i=1..n) / n
```

```python
mean_face = np.sum(X_train, axis=0) / n_train
X_train_centered = X_train - mean_face
X_test_centered = X_test - mean_face
```

**چرا mean آزمون را جدا حساب نمی‌کنیم؟**

در زمان استفاده واقعی، مدل فقط پارامترهای یادگرفته‌شده از train را دارد. استفاده از mean آزمون یعنی اطلاعات آزمون را پیش از ارزیابی دیده‌ایم.

**آزمون صحت:**

```python
assert np.allclose(X_train_centered.mean(axis=0), 0, atol=1e-12)
```

لازم نیست میانگین `X_test_centered` صفر باشد. صفرنبودن آن خطا نیست.

### تصمیم ۶: واریانس نمونه و تقسیم بر `n-1`

**نوع:** تصمیم آماری سازگار با صورت پروژه

```text
Var(x) = sum((x[i] - mean(x))²) / (n - 1)
```

و برای کل ماتریس مرکز‌شده:

```text
C = (X_centered.T @ X_centered) / (n - 1)
```

در این فرمول، درایه‌های قطر `C` واریانس هر پیکسل و درایه‌های خارج از قطر کوواریانس میان جفت پیکسل‌ها هستند. بنابراین ساخت همین ماتریس، محاسبه هم‌زمان واریانس و کوواریانس روی داده‌های واقعی است.

**چرا `n-1`؟**

چون mean را از همین نمونه‌ها برآورد کرده‌ایم، covariance نمونه معمولاً با درجه آزادی `n-1` تعریف می‌شود.

**نکته مهم:** اگر همه‌جا به‌صورت سازگار بر `n` تقسیم کنیم، جهت eigenvectorها تغییر نمی‌کند و فقط eigenvalueها مقیاس می‌خورند. ما `n-1` را انتخاب کرده‌ایم چون با تعریف sample covariance و صورت پروژه سازگارتر است.

### چرا مثال‌های عددی ساختگی را از نوت‌بوک حذف کردیم؟

برای یادگیری اولیه می‌توان واریانس و کوواریانس را روی آرایه‌هایی مانند `[1, 2, 3, 4, 5]` محاسبه کرد؛ اما صورت پروژه چنین مثالی را به‌عنوان خروجی نخواسته است. این مثال‌ها به دیتاست چهره وصل نبودند و مسیر اصلی نوت‌بوک را قطع می‌کردند. به همین دلیل، تعریف و دلیل فرمول در این راهنمای آموزشی نگه داشته شد و نوت‌بوک فقط محاسبه واقعی روی `X_train_centered` را انجام می‌دهد.

هدف حذف ریاضیات نیست؛ هدف این است که هر محاسبه داخل نوت‌بوک مستقیماً به داده و خروجی پروژه مربوط باشد.

### واریانس کل داده واقعی

لازم نیست برای به‌دست‌آوردن مجموع واریانس، ماتریس بزرگ `C` را بسازیم. مجموع واریانس تمام پیکسل‌ها از مجموع مربع تمام درایه‌های داده مرکز‌شده به دست می‌آید:

```text
total_variance = sum(X_centered[i, j]²) / (n - 1)
```

پیاده‌سازی استفاده‌شده در نوت‌بوک:

```python
total_variance = np.sum(X_train_centered ** 2) / (n_train - 1)
```

از نظر ماتریسی، این مقدار با trace ماتریس covariance برابر است:

```text
total_variance = trace(C)
```

**آزمون نظری در صورت ساختن covariance:** قطر covariance باید برابر واریانس ستون‌ها باشد.

```python
assert np.allclose(np.diag(C), X_train_centered.var(axis=0, ddof=1))
```

ما این تست را داخل نوت‌بوک اجرا نمی‌کنیم، چون به ساخت ماتریس `4096×4096` نیاز دارد و هدف استفاده از Gram matrix، اجتناب از همین محاسبه بزرگ است.

### تصمیم ۷: استفاده از Gram matrix به‌جای covariance بزرگ

**نوع:** تصمیم محاسباتی که صریحاً در صورت پروژه پیشنهاد شده

### آیا صورت پروژه استفاده از Gram matrix را گفته است؟

بله. در صفحه ۳ فایل `Math-Project-Kickoff.pdf`، گام ۵ مسیر الزامی پروژه می‌گوید ماتریس covariance **یا** Gram matrix را بسازید، تجزیه ویژه را انجام دهید و جهت‌ها را نزولی مرتب کنید. پس هر دو مسیر مجازند.

در صفحه ۱۰ فایل `Math-Project-Kickoff-Eigenfaces-Rahnema-College.pdf` نیز روش مناسب برای حالت `d >> n` توضیح داده شده است: مسئله ویژه کوچک‌تر `n×n` را روی Gram matrix داده train حل کنید و سپس جهت‌ها را به فضای اصلی برگردانید. در همان اسلاید به استفاده از `eigh` روی Gram matrix اشاره شده است.

بنابراین Gram matrix میان‌بری خارج از پروژه نیست؛ یکی از دو مسیر رسمی و پیشنهادی پروژه است.

### چرا برای داده ما انتخاب مناسبی است؟

داده مرکز‌شده train این شکل را دارد:

```text
X_train_centered.shape = (320, 4096)
```

یعنی:

```text
n = 320     number of train images
d = 4096    number of pixels in each image
```

در این پروژه تعداد ویژگی‌ها بسیار بیشتر از تعداد نمونه‌هاست:

```text
4096 >> 320
```

اگر covariance پیکسل‌ها را بسازیم:

```text
C = (X_centered.T @ X_centered) / (n - 1)
C.shape = (4096, 4096)
```

این ماتریس `16,777,216` درایه دارد. اما Gram matrix چنین است:

```text
G = (X_centered @ X_centered.T) / (n - 1)
G.shape = (320, 320)
```

Gram matrix فقط `102,400` درایه دارد؛ یعنی تعداد درایه‌های آن حدود ۱۶۴ برابر کمتر است. بنابراین نگهداری ماتریس و اجرای eigendecomposition روی آن بسیار سبک‌تر می‌شود.

### آیا Gram matrix همان covariance matrix است؟

خیر. این دو شکل و معنای متفاوتی دارند:

- `C` ارتباط میان ویژگی‌ها یا پیکسل‌ها را نشان می‌دهد و شکل آن `d×d` است.
- `G` ارتباط میان نمونه‌های train را نشان می‌دهد و شکل آن `n×n` است.

پس نباید در توضیح یا ارائه، Gram matrix را «همان covariance پیکسل‌ها» بنامیم. نکته درست این است که این دو ماتریس، eigenvalueهای غیرصفر یکسان و جهت‌های اصلی متناظر دارند.

### چرا نتیجه غیرصفر PCA یکسان می‌ماند؟

اگر:

```text
G @ v = lambda * v
```

اکنون دو طرف معادله را در ترانهاده داده‌های مرکز‌شده ضرب می‌کنیم:

```text
C @ (X_centered.T @ v) = lambda * (X_centered.T @ v)
```

پس بردار زیر، یک بردار ویژه از ماتریس کوواریانس بزرگ است:

```text
X_centered.T @ v
```

مقدار ویژه آن نیز با مقدار ویژه غیرصفر ماتریس گرام برابر است. در پایان، طول این بردار را برابر یک می‌کنیم.

کد پروژه همین تبدیل را انجام می‌دهد:

```python
eigenfaces = X_train_centered.T @ eigenvectors

lengths = np.sqrt(np.sum(eigenfaces ** 2, axis=0))
eigenfaces = eigenfaces / lengths
```

پس ابتدا مسئله کوچک‌تر را حل می‌کنیم و سپس بردارهای ویژه را به فضای ۴۰۹۶ پیکسلی برمی‌گردانیم.

یک کنترل مستقیم روی داده واقعی این است که مجموع eigenvalueهای غیرصفر Gram با واریانس کل داده برابر باشد:

```python
variance_is_preserved = np.allclose(
    np.sum(eigenvalues),
    total_variance,
)
```

دلیل این برابری آن است که مجموع eigenvalueهای یک ماتریس متقارن با trace آن برابر است و trace ماتریس Gram نیز با trace ماتریس covariance برابر می‌شود. در این پروژه، این کنترل نشان می‌دهد واریانس موجود در داده train هنگام رفتن از مسیر covariance به مسیر Gram گم نشده است.

**چرا حداکثر ۳۱۹ مؤلفه مفید داریم؟**

مرکزسازی یک وابستگی خطی ایجاد می‌کند؛ جمع ردیف‌های داده مرکز‌شده صفر است. بنابراین rank حداکثر `n_train - 1 = 319` است.

Gram matrix نیز در اجرای فعلی دقیقاً ۳۱۹ eigenvalue مفید تولید کرده است. بنابراین استفاده از ماتریس کوچک‌تر باعث حذف هیچ جهت PCA غیرصفری نشده است.

### آیا Gram matrix همیشه انتخاب بهتری است؟

خیر. انتخاب به اندازه داده بستگی دارد:

```text
اگر d >> n باشد: Gram matrix معمولاً کوچک‌تر است.
اگر n >> d باشد: covariance matrix ممکن است کوچک‌تر باشد.
```

برای این پروژه `d = 4096` و `n = 320` است؛ بنابراین Gram matrix انتخاب منطقی‌تری است.

### جمله کوتاه برای ارائه

> چون هر تصویر ۴۰۹۶ ویژگی دارد ولی فقط ۳۲۰ تصویر آموزشی داریم، به‌جای حل مسئله بزرگ `4096×4096`، مسئله کوچک‌تر `320×320` را حل می‌کنیم و بردارها را دوباره به فضای تصویر برمی‌گردانیم.

### تصمیم ۸: استفاده از `np.linalg.eigh`

**نوع:** مجاز طبق صورت پروژه و مناسب از نظر عددی

Gram و covariance ماتریس‌های حقیقی متقارن‌اند. `eigh` مخصوص ماتریس‌های متقارن است و eigenvalueهای حقیقی و eigenvectorهای نرمال‌شده برمی‌گرداند.

**چرا `eig` نه؟**

`eig` برای ماتریس عمومی است و از ساختار متقارن مسئله استفاده نمی‌کند. برای این مسئله `eigh` انتخاب روشن‌تر و مناسب‌تری است.

**نکته خروجی:** `eigh` eigenvalueها را صعودی می‌دهد؛ PCA به ترتیب نزولی نیاز دارد.

```python
order = np.argsort(eigenvalues)[::-1]
eigenvalues = eigenvalues[order]
eigenvectors = eigenvectors[:, order]
```

**آزمون‌های صحت:**

```python
assert np.allclose(L, L.T)
assert np.all(np.diff(eigenvalues) <= 0)
assert np.allclose(
    eigenfaces.T @ eigenfaces,
    np.eye(eigenfaces.shape[1]),
    atol=1e-10,
)
```

ممکن است چند eigenvalue منفی بسیار کوچک در حد خطای floating-point دیده شود. آن‌ها و مؤلفه‌های تقریباً صفر با threshold نسبی حذف می‌شوند، نه با یک عدد بزرگ دلخواه.

### تصمیم ۹: projection به فضای کم‌بعد

**نوع:** الزام پروژه

اگر `U_k` شامل اولین `k` eigenface باشد:

```text
Z = X_centered @ U_k
```

ابعاد:

```text
X_c: (n, 4096)
U_k: (4096, k)
Z:   (n, k)
```

هر ردیف `Z` کد فشرده یک چهره است.

**آزمون صحت:**

```python
assert train_codes.shape == (320, k)
assert test_codes.shape == (80, k)
```

### تصمیم ۱۰: reconstruction

**نوع:** الزام پروژه

```text
X_reconstructed = Z @ U_k.T + mean_face
```

ابتدا از فضای `k`بعدی به فضای ۴۰۹۶بعدی برمی‌گردیم و سپس mean face را اضافه می‌کنیم.

**اشتباه رایج:** فراموش‌کردن اضافه‌کردن mean باعث می‌شود خروجی فقط deviation از میانگین باشد، نه تصویر بازسازی‌شده.

**آزمون صحت:** برای eigenfaceهای مرتب‌شده، با افزایش `k` خطای بازسازی نباید زیاد شود؛ اختلاف بسیار کوچک در حد خطای اعشاری قابل‌قبول است.

### تصمیم ۱۱: استفاده از MSE برای خطای بازسازی

**نوع:** تصمیم تیم، سازگار با هدف پروژه

```text
MSE = sum((X - X_reconstructed)²) / (n * d)
```

**چرا؟**

MSE ساده، قابل‌توضیح و مستقیماً با هدف کمینه‌سازی خطای مربعی PCA هماهنگ است.

**محدودیت:** MSE الزاماً با کیفیت ادراکی تصویر برابر نیست؛ به همین علت مقایسه بصری نیز گزارش می‌کنیم.

### تصمیم ۱۲: nearest neighbor از صفر

**نوع:** الزام پروژه

برای هر کد test، فاصله یا شباهت آن را با تمام کدهای train حساب می‌کنیم و label نزدیک‌ترین نمونه را برمی‌گردانیم. این روش هیچ پارامتر یادگرفتنی اضافه‌ای ندارد.

**Euclidean distance:**

```text
d(a, b) = sqrt(sum((a[j] - b[j])²))
```

کمترین فاصله انتخاب می‌شود. برای مقایسه همسایه‌ها می‌توان ریشه دوم را حذف کرد، چون ترتیب فاصله‌ها تغییر نمی‌کند.

**Cosine similarity:**

```text
s(a, b) = (a.T @ b) / (norm(a) * norm(b))
```

بیشترین شباهت انتخاب می‌شود. تقسیم بر صفر باید با بررسی norm یا یک epsilon بسیار کوچک کنترل شود.

### تصمیم ۱۳: مقایسه Euclidean و Cosine بدون whitening در آزمایش اصلی

**نوع:** طراحی آزمایش تیم ۴

در فضای PCA، واریانس مؤلفه `j` برابر eigenvalue همان مؤلفه است. بنابراین مؤلفه‌های ابتدایی معمولاً مقیاس بزرگ‌تری دارند.

- Euclidean به اختلاف عددی مختصات حساس است؛ مؤلفه‌های با تغییرات بزرگ‌تر می‌توانند سهم بیشتری داشته باشند.
- Cosine طول کل بردار را حذف و بیشتر جهت دو بردار را مقایسه می‌کند.
- Cosine به‌تنهایی تک‌تک مؤلفه‌ها را هم‌مقیاس یا whiten نمی‌کند.

برای مقایسه اصلی، هر دو معیار باید دقیقاً روی یک `train_codes` و `test_codes` و بدون preprocessing متفاوت اجرا شوند؛ وگرنه فقط معیار فاصله را مقایسه نکرده‌ایم.

**آزمایش اختیاری:** whitening با تقسیم هر مؤلفه بر `sqrt(eigenvalue[j])` موضوع جالبی است، اما سؤال متفاوتی می‌سازد. اگر انجام شود باید جدا و با برچسب آزمایش تکمیلی گزارش شود.

### تصمیم ۱۴: مقادیر `k` از پیش تعیین‌شده

**نوع:** `10, 50, 150` الزام پروژه؛ مقادیر اضافه انتخاب تیم

مقادیر اصلی قبل از دیدن accuracy آزمون تعیین می‌شوند. برای نمودار دقیق‌تر می‌توان مجموعه زیر را نیز اجرا کرد:

```text
1, 5, 10, 20, 50, 100, 150, 250
```

**چرا؟**

اگر بارها با test امتحان کنیم و فقط بهترین `k` را گزارش کنیم، test عملاً به validation تبدیل می‌شود و accuracy نهایی خوش‌بینانه خواهد بود.

در این پروژه سه مقدار الزامی را کامل گزارش می‌کنیم و از پنهان‌کردن نتایج ضعیف خودداری می‌کنیم.

### تصمیم ۱۵: مقایسه حداقلی با SVD

**نوع:** الزام مشترک پروژه

برای داده مرکز‌شده:

```text
X_centered = Q @ Sigma @ V.T
```

ردیف‌های `V^T` جهت‌های اصلی در فضای پیکسل‌اند و رابطه eigenvalueها چنین است:

```text
eigenvalue[j] = singular_value[j]² / (n - 1)
```

**چرا بردارها را پیکسل‌به‌پیکسل مقایسه نمی‌کنیم؟**

علامت eigenvector دلخواه است؛ `u` و `-u` یک جهت را نمایش می‌دهند. در eigenvalueهای تکراری حتی پایه‌های متفاوت می‌توانند همان زیرفضا را بسازند. معیار بهتر، reconstruction error یا فاصله بین projection matrixهای دو زیرفضاست.

### تصمیم ۱۶: accuracy، موفقیت و شکست

**نوع:** الزام پروژه

```text
Accuracy = correct_predictions / number_of_test_samples
```

فقط accuracy کافی نیست. باید حداقل این موارد را ببینیم:

- یک نمونه درست و نزدیک‌ترین تصویر train آن؛
- یک نمونه اشتباه و نزدیک‌ترین تصویر train آن؛
- چند نمونه که Euclidean و Cosine همسایه متفاوت انتخاب کرده‌اند؛
- توضیح محتاطانه درباره نور، حالت چهره، عینک یا زاویه.

نباید از یک نمونه، قانون کلی درباره علت شکست استخراج کنیم؛ توضیح ما یک احتمال مشاهده‌شده است.

## ۴. ممیزی نوت‌بوک فعلی

این ممیزی مربوط به نسخه فعلی پیش از تکمیل نیمه دوم پروژه است.

### موارد تأییدشده با اجرای عددی

| کنترل | نتیجه |
|---|---:|
| شکل train | `(320, 4096)` |
| شکل test | `(80, 4096)` |
| بزرگ‌ترین قدرمطلق mean بعد از centering | حدود `2.25e-16` |
| بیشترین عدم تقارن Gram | `0.0` |
| تعداد مؤلفه غیرصفر | `319` |
| ترتیب eigenvalueها | نزولی و صحیح |
| بیشترین خطای طول واحد eigenfaceها | حدود `4.00e-15` |
| بیشترین خطای orthogonality | حدود `8.65e-14` |
| اختلاف مجموع eigenvalueها با total variance | `0.0` در اجرای ممیزی |
| بیشترین residual نسبی معادله ویژه برای ۱۰ مؤلفه اول | حدود `6.92e-15` |
| استفاده از PCA آماده | مشاهده نشد |
| استفاده از nearest neighbor آماده | مشاهده نشد |
| اعتبار ساختار JSON نوت‌بوک | تأیید شد |

این خطاها در مقیاس محاسبات `float64` بسیار کوچک‌اند و هسته فعلی از نظر عددی درست عمل می‌کند.

این اجرا با نسخه‌های `NumPy 2.5.2`، `scikit-learn 1.9.0` و `Matplotlib 3.11.1` انجام شده است. فایل `requirements.txt` فعلاً حداقل نسخه‌ها را مشخص می‌کند تا نصب اعضای تیم ساده‌تر باشد. پیش از تحویل نهایی، نسخه‌های دقیق محیطی که Run All موفق داشته است نیز ثبت می‌شوند تا بازتولید نتیجه مطمئن‌تر شود.

### موارد نیازمند اصلاح یا تکمیل

موارد مربوط به split تصادفی، ترتیب split پیش از flatten، نمایش کنترل orthogonality و حذف سلول خالی اصلاح شده‌اند. کارهای باقی‌مانده:

1. projection و reconstruction وجود ندارند.
2. reconstruction error و نمودار آن وجود ندارند.
3. nearest neighbor و accuracy وجود ندارند.
4. آزمایش Euclidean/Cosine وجود ندارد.
5. مقایسه SVD وجود ندارد.
6. نمونه موفق، شکست و تحلیل محدودیت وجود ندارند.
7. پس از تکمیل، اجرای واقعی Restart Kernel and Run All باید انجام شود.

بنابراین عبارت دقیق این است: **هسته ریاضی موجود درست است، اما پروژه هنوز کامل نیست.**

## ۵. تست‌هایی که باید داخل نوت‌بوک بمانند

تست‌ها فقط برای توسعه نیستند؛ بخشی از شواهد ارائه هستند.

```python
# Split
assert X_train.shape == (320, 4096)
assert X_test.shape == (80, 4096)
assert np.all(np.bincount(y_train) == 8)
assert np.all(np.bincount(y_test) == 2)

# Centering
assert np.allclose(X_train_centered.mean(axis=0), 0, atol=1e-12)

# Gram matrix
assert np.allclose(gram_matrix, gram_matrix.T, atol=1e-12)

# Ordered eigenvalues
assert np.all(np.diff(eigenvalues) <= 0)

# Total variance from the real train data
total_variance = np.sum(X_train_centered ** 2) / (n_train - 1)
assert np.allclose(np.sum(eigenvalues), total_variance)

# Orthonormal eigenfaces
identity = np.eye(eigenfaces.shape[1])
assert np.allclose(eigenfaces.T @ eigenfaces, identity, atol=1e-10)

# Compressed shapes
assert train_codes.shape == (320, k)
assert test_codes.shape == (80, k)

# Predictions
assert predictions.shape == y_test.shape
assert np.all((predictions >= 0) & (predictions < 40))
```

## ۶. خطاهای رایجی که باید مراقبشان باشیم

- محاسبه mean با کل ۴۰۰ تصویر
- ساخت eigenfaces با test
- اشتباه‌گرفتن row و column در ماتریس داده
- استفاده از `eig` بدون توجه به ساختار متقارن
- فراموش‌کردن مرتب‌سازی eigenvalue و eigenvector با یک index مشترک
- تقسیم eigenface بر norm صفر یا بسیار کوچک
- استفاده از eigenvectorهای تقریباً صفر
- projection داده test با mean متفاوت
- فراموش‌کردن mean هنگام reconstruction
- انتخاب `k` با نگاه مکرر به نتیجه test
- مقایسه Euclidean و Cosine روی داده‌های متفاوت
- انتخاب کمینه برای Cosine درحالی‌که باید بیشینه انتخاب شود
- گزارش accuracy آموزش به‌جای test
- نمایش فقط نمونه‌های موفق
- تفسیر قطعی یک eigenface به‌عنوان «چشم» یا «لبخند»
- مقایسه مستقیم علامت بردارهای مسیر eigh و SVD

## ۷. شیوه دفاع از پروژه در ارائه

برای هر ادعا چهار سؤال را پاسخ می‌دهیم:

1. **ادعا چیست؟** مثال: Cosine برای این split دقت بیشتری داشت.
2. **شاهد چیست؟** عدد accuracy، جدول یا نمونه تصویر.
3. **آزمایش چگونه منصفانه بود؟** همان split، همان `k` و همان PCA برای هر دو معیار.
4. **محدودیت چیست؟** دیتاست کوچک و فقط یک split ثابت.

عبارت‌های مناسب:

- «در split ثابت این آزمایش مشاهده کردیم که ...»
- «یک علت احتمالی این تفاوت می‌تواند ... باشد.»
- «این نتیجه برای همه دیتاست‌ها قابل تعمیم نیست.»

عبارت‌های نامناسب:

- «Cosine همیشه بهتر است.»
- «این eigenface قطعاً مربوط به چشم است.»
- «چون کد بدون خطا اجرا شد، مدل درست است.»

## ۸. منابع راستی‌آزمایی

### منابع پروژه

- `Math-Project-Kickoff.pdf`، به‌ویژه صفحات ۳ تا ۱۰
- `Math-Project-Kickoff-Eigenfaces-Rahnema-College.pdf`، به‌ویژه صفحات ۳ تا ۱۷
- فایل برنامه ویدیوهای درس ریاضی برای مرور مباحث پیش‌نیاز

### منابع فنی

- [مستندات رسمی دیتاست Olivetti در scikit-learn](https://scikit-learn.org/stable/datasets/real_world.html#the-olivetti-faces-dataset)
- [مستندات رسمی `numpy.linalg.eigh`](https://numpy.org/doc/stable/reference/generated/numpy.linalg.eigh.html)
- [مستندات رسمی `numpy.linalg.svd`](https://numpy.org/doc/stable/reference/generated/numpy.linalg.svd.html)
- [مستندات رسمی مولد تصادفی NumPy](https://numpy.org/doc/stable/reference/random/generator.html)
- [مقاله Eigenfaces for Recognition از Turk و Pentland](https://courses.media.mit.edu/2008fall/mas622j/Turk%2BPentland-1991.pdf)

## ۹. روش نگهداری این سند

پس از هر تغییر مهم، یک ردیف به جدول زیر اضافه می‌کنیم:

| تاریخ | تصمیم یا تغییر | دلیل | شاهد یا تست | مسئول |
|---|---|---|---|---|
| ۱۴۰۵/۰۶/۱۴ | انتخاب split تصادفی درون هر هویت با seed=42 | تکرارپذیری و حذف وابستگی به ترتیب داده | شمارش ۸/۲ و نبود هم‌پوشانی | تیم ۴ |
| ۱۴۰۵/۰۶/۱۴ | استفاده از Gram matrix | کاهش مسئله ویژه از `4096×4096` به `320×320` | معادله ویژه و کنترل زیرفضا | تیم ۴ |
| ۱۴۰۵/۰۶/۱۴ | مقایسه بدون whitening در آزمایش اصلی | تغییر فقط معیار شباهت در مقایسه منصفانه | یکسان‌بودن codeهای ورودی دو معیار | تیم ۴ |
| ۱۴۰۵/۰۶/۱۵ | حذف مثال‌های ساختگی واریانس و کوواریانس از نوت‌بوک | تمرکز نوت‌بوک بر محاسبات واقعی پروژه | برابری total variance با مجموع eigenvalueها | تیم ۴ |

هر تصمیم جدید باید به یکی از این چهار دسته تعلق داشته باشد: الزام استاد، تصمیم علمی، تصمیم مهندسی یا محدودیت زمانی. اگر نتوانیم نوع و دلیل آن را بنویسیم، هنوز تصمیم به‌اندازه کافی روشن نیست.

</div>
